# Credenciales. El .env.example sí se versiona; el .env real, nunca.
.env

# Entorno y caché de Python
.venv/
__pycache__/
*.pyc
.ruff_cache/

# Bases de datos locales de ejecución
*.db
*.sqlite3

# Audio generado en pruebas
*.wav

# Turnos transcritos de llamadas reales (REGISTRO_TURNOS): la voz de alguien
registros/

# Voces de Piper (artefactos de terceros con licencia propia, se descargan) y la
# caché vieja del audio. El audio de las frases fijas sí se versiona, en
# audio/frases/ (ver server/voz/tts.py).
voces/

# Notas de trabajo, no van al repo
notas/
